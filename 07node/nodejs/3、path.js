const fs = require('fs');
const path = require('path');

// resolve 拼接规范的绝对路径
// console.log(path.resolve(__dirname, 'path.js'));

// path.join 拼接路径
// console.log(path.join(__dirname, 'path.js'));

// 当前文件的绝对路径
// console.log(__filename); 

// path.dirname 获取文件所在目录
// console.log(path.dirname(__filename));

// parse 解析路径并返回对象
// console.log(path.parse(__filename));

// path.basename 获取文件名
// console.log(path.basename(__filename));

// path.extname 获取文件扩展名
// console.log(path.extname(__filename));

// path.relative 获取相对路径
// console.log(path.relative(__dirname, 'path.js'));

// path.isAbsolute 判断是否是绝对路径
// console.log(path.isAbsolute(__dirname));

// sep 获取操作系统的路径分隔符 windows \ linux /
// console.log(path.sep);
